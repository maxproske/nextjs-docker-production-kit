# DO NOT ADD SECRETS TO THIS FILE. This is a good place for defaults.
# If you want to add secrets use any of the following filenames instead, 
# which are automatically detected by Docker Compose:
# .env.local
# .env.development.local
# .env.test.local
# .env.production.local

ENV_VARIABLE=production_server_only_variable
NEXT_PUBLIC_ENV_VARIABLE=production_public_variable

POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_DATABASE=saas
POSTGRES_USER=saas_user
POSTGRES_PASSWORD=password
